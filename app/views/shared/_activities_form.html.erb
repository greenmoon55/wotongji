<% if @activities.any? %>
  <%= will_paginate @activities %>
  <div class="activities">
    <%= render partial: 'shared/activity', collection: @activities %>
  </div>
  <%= will_paginate @activities %>
<% end %>
