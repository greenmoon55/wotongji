<%= form_for(current_user.interests.build(activity_id: @activity.id),
             remote: true) do |f| %>
  <%= f.hidden_field :activity_id %>
  <%= f.submit "　感兴趣　", class: "btn btn-primary" %>
<% end %>
