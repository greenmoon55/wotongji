$("#activities").html("<%= escape_javascript(render('shared/activities_form')) %>")
$("#past").live('ajax:beforeSend', function(event, xhr, settings) {
  if(history && history.pushState) {
    history.pushState(null, document.title, '/activities/past');
  }
});
